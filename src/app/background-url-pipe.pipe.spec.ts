import { BackgroundUrlPipePipe } from './background-url-pipe.pipe';

describe('BackgroundUrlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BackgroundUrlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
